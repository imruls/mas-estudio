---
interface Props {
  label: string;
  href: string;
  variant?: "cta";
}

const { label, href, variant } = Astro.props;
---

<a href={href} class:list={["nav-link", { "nav-link--cta": variant === "cta" }]}>
  {label}
</a>
